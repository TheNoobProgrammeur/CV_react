import flaskIcon from "@iconify-icons/cib/flask"
import graphqlIcon from "@iconify-icons/cib/graphql"
import springIcon from "@iconify-icons/cib/spring"
import kotlinIcone from "@iconify-icons/cib/kotlin"
import latxIcone from "@iconify-icons/cib/latex"
import firebaseIcone from "@iconify-icons/cib/firebase"
import netlifyIcone from "@iconify-icons/cib/netlify"
import circleIcone from "@iconify-icons/cib/circleci"

import { Icon } from "@iconify/react"
import React from "react"

const flask = (
  <i>
    <Icon icon={flaskIcon} />
  </i>
)
const graphql = (
  <i>
    <Icon icon={graphqlIcon} />
  </i>
)
const spring = (
  <i>
    <Icon icon={springIcon} />
  </i>
)
const kotlin = (
  <i>
    <Icon icon={kotlinIcone} />
  </i>
)
const latex = (
  <i>
    <Icon icon={latxIcone} />
  </i>
)
const firebase = (
  <i>
    <Icon icon={firebaseIcone} />
  </i>
)
const netlify = (
  <i>
    <Icon icon={netlifyIcone} />
  </i>
)
const circle = (
  <i>
    <Icon icon={circleIcone} />
  </i>
)

export { flask, graphql, spring, kotlin, latex, firebase, netlify, circle }
